# Secret Santa Bot 🎅

Telegram-бот для организации игры в "Тайного Санту" с возможностью создания вишлистов, блокировки нежелательных участников и автоматического распределения получателей подарков.

## Возможности

### Для участников
- 🎯 **Регистрация** - автоматическая регистрация при первом запуске
- 📝 **Вишлист** - создание списка желаемых подарков
- 🚫 **Блок-лист** - блокировка нежелательных участников для распределения
- 🎁 **Просмотр назначения** - информация о том, кому дарить подарок
- 👥 **Список участников** - просмотр всех зарегистрированных пользователей

### Для администраторов
- 🎲 **Распределение** - автоматическое назначение получателей подарков
- ♻️ **Сброс результатов** - возможность переделать распределение
- 🗑️ **Полный сброс** - очистка всех данных
- 📤 **Экспорт данных** - выгрузка состояния в JSON формате
- 📊 **Статистика** - количество участников и статус распределения

## Технические особенности

- 🏗️ **Clean Architecture** - разделение на слои domain, application, infrastructure, presentation
- 💾 **Persistent Storage** - сохранение состояния в JSON файлы
- 🔒 **Безопасность** - проверка прав администратора
- ⚙️ **Конфигурация** - настройка через .env файлы
- 🎯 **Smart Distribution** - алгоритм учитывающий блок-листы участников
- 📱 **Inline UI** - современный интерфейс с кнопками

## Установка и настройка

### Требования
- Dart SDK 3.3.0+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

### Быстрый старт

1. **Клонирование проекта:**
   ```bash
   git clone <repository_url>
   cd secret_santa_bot
   ```

2. **Установка зависимостей:**
   ```bash
   dart pub get
   ```

3. **Настройка конфигурации:**
   ```bash
   cp .env.example .env
   ```

   Отредактируйте `.env` файл:
   ```env
   BOT_TOKEN=your_bot_token_here
   ADMIN_ID=your_telegram_id_here
   DATA_DIR=data
   LOG_LEVEL=info
   MAX_ATTEMPTS=2000
   STATE_FILE=state.json
   ENVIRONMENT=development
   ```

4. **Запуск бота:**
   ```bash
   dart run
   ```

### Получение Telegram Bot Token

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env` файл

### Получение Admin ID

1. Запустите бота
2. Отправьте команду `/admin_check`
3. Скопируйте ваш ID из ответа в поле `ADMIN_ID` в `.env`
4. Перезапустите бота

## Структура проекта

```
secret_santa_bot/
├── bin/
│   └── main.dart                 # Точка входа приложения
├── lib/
│   ├── domain/                   # Бизнес-логика
│   │   ├── models.dart          # Модели данных
│   │   └── repositories.dart    # Интерфейсы репозиториев
│   ├── app/                     # Сервисы приложения
│   │   └── secret_santa_service.dart
│   ├── infrastructure/          # Внешние зависимости
│   │   ├── config.dart         # Конфигурация
│   │   └── json_state_store.dart # Хранение данных
│   └── presentation/            # UI слой
│       ├── handlers.dart       # Обработчики команд
│       └── ui.dart            # UI компоненты
├── data/                       # Данные бота
│   ├── state.json             # Состояние участников
│   └── error.log             # Логи ошибок
├── scripts/                   # Скрипты запуска
│   ├── run.sh               # Запуск для Linux/Mac
│   └── run.bat             # Запуск для Windows
├── .env                    # Конфигурация (не в git)
├── .env.example           # Шаблон конфигурации
├── pubspec.yaml          # Зависимости Dart
└── README.md            # Документация
```

## Использование

### Команды бота

- `/start` - Регистрация и главное меню
- `/members` - Список участников
- `/admin_check` - Проверка прав администратора (отладка)

### Игровой процесс

1. **Регистрация участников** - каждый участник отправляет `/start`
2. **Настройка вишлистов** - участники заполняют свои желания
3. **Настройка блок-листов** - (опционально) блокировка нежелательных участников
4. **Распределение** - администратор запускает распределение (минимум 3 участника)
5. **Получение назначений** - каждый участник видит своего получателя и его вишлист

### Алгоритм распределения

Бот использует умный алгоритм, который:
- Создает кольцевое распределение (каждый дарит одному и получает от одного)
- Учитывает блок-листы участников
- Пытается найти валидное распределение до 2000 попыток
- Исключает самоназначения

## Конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `BOT_TOKEN` | Токен Telegram бота | **обязательно** |
| `ADMIN_ID` | ID администратора | **обязательно** |
| `DATA_DIR` | Директория для данных | `data` |
| `STATE_FILE` | Файл состояния | `state.json` |
| `LOG_LEVEL` | Уровень логирования | `info` |
| `MAX_ATTEMPTS` | Попытки распределения | `2000` |
| `ENVIRONMENT` | Окружение | `development` |

### Структура данных

Состояние сохраняется в JSON формате:
```json
{
  "users": {
    "123456789": {
      "id": 123456789,
      "name": "Иван Иванов",
      "blocked": [987654321],
      "wishlist": "Книги, настольные игры",
      "assignedTo": 555666777
    }
  },
  "distributed": true,
  "updatedAt": "2025-01-01T00:00:00.000Z"
}
```

## Разработка

### Архитектура

Проект следует принципам Clean Architecture:

- **Domain** - бизнес-логика, модели, интерфейсы
- **Application** - сервисы приложения, use cases
- **Infrastructure** - внешние зависимости (файлы, API)
- **Presentation** - UI, обработчики команд

### Добавление функций

1. **Новые команды** - добавить в `_setupHandlers()` в `main.dart`
2. **UI элементы** - создать методы в `lib/presentation/ui.dart`
3. **Бизнес-логика** - расширить `SecretSantaService`
4. **Хранение** - обновить модели в `domain/models.dart`

### Тестирование

Для отладки используйте:
- Команду `/admin_check` для проверки прав
- Консольные логи с отладочной информацией
- Файл `data/error.log` для ошибок

## Деплой

### Локальный запуск

```bash
# Linux/Mac
chmod +x scripts/run.sh
./scripts/run.sh

# Windows
scripts/run.bat

# Прямой запуск
dart run
```

### Продакшен

1. Установите `ENVIRONMENT=production` в `.env`
2. Настройте systemd service или docker container
3. Обеспечьте backup директории `data/`
4. Настройте мониторинг логов

### Docker (опционально)

```dockerfile
FROM dart:stable

WORKDIR /app
COPY pubspec.* ./
RUN dart pub get

COPY . .
RUN dart pub get --offline

EXPOSE 8080
CMD ["dart", "run"]
```

### Systemd Service

```ini
[Unit]
Description=Secret Santa Telegram Bot
After=network.target

[Service]
Type=simple
User=botuser
WorkingDirectory=/path/to/secret_santa_bot
ExecStart=/usr/lib/dart/bin/dart run
Restart=always
RestartSec=10
EnvironmentFile=/path/to/secret_santa_bot/.env

[Install]
WantedBy=multi-user.target
```

## Безопасность

- 🔒 Токен бота хранится в .env (не в git)
- 🛡️ Проверка прав администратора для критичных операций
- 📝 Логирование всех ошибок
- 🚫 Валидация входных данных
- 🔐 Изоляция пользователей (нет доступа к чужим данным)

## Часто задаваемые вопросы

**Q: Бот не показывает кнопки**
A: Проверьте версию Televerse в pubspec.yaml, убедитесь что используется правильный синтаксис InlineKeyboard

**Q: Нет прав администратора**
A: Используйте `/admin_check` для проверки ID, обновите ADMIN_ID в .env и перезапустите бота

**Q: Не удается распределить участников**
A: Проверьте блок-листы - возможно, они создают невозможную ситуацию для распределения

**Q: Участник не получил уведомление**
A: Пользователь должен сначала написать боту /start в личных сообщениях

## Зависимости

```yaml
dependencies:
  televerse: ^3.0.1  # Telegram Bot API
  path: ^1.9.0      # Работа с путями
  dotenv: ^4.2.0    # Загрузка .env файлов
```

## Лицензия

MIT License - см. файл LICENSE

## Поддержка

При возникновении проблем:

1. Проверьте логи в `data/error.log`
2. Используйте команду `/admin_check` для диагностики
3. Убедитесь в правильности конфигурации `.env`
4. Проверьте права доступа к директории `data/`

---

**Создано с ❤️ для организации новогодних праздников!** 🎄
